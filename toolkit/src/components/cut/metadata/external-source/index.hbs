{{#if @externalSource}}
  <!-- TODO: add translation(CC-5314) -->
  <Cut::TextWithIcon
    @text={{concat "Registered via " @externalSource}}
    @icon={{this.externalSourceIcon}}
    @iconColor="var(--token-color-hashicorp-brand)"
    data-test-external-source={{@service.metadata.externalSource}}
  />
{{/if}}
